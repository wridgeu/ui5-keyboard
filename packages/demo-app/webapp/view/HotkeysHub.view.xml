<mvc:View
    controllerName="demo.hotkeys.controller.HotkeysHub"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">
    <Page title="Hotkey Manager Demos" showNavButton="true" navButtonPress=".onNavBack" class="sapUiContentPadding">
        <content>
            <List
                headerText="Choose a Scenario"
                items="{state>/hotkeysDemoEntries}"
                itemPress=".onScenarioPress"
                inset="true">
                <StandardListItem
                    title="{state>title}"
                    description="{state>description}"
                    type="Navigation" />
            </List>
        </content>
    </Page>
</mvc:View>
