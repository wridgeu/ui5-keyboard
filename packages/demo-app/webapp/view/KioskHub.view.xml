<mvc:View
    controllerName="demo.hotkeys.controller.KioskHub"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">
    <Page titleAlignment="Center" title="Kiosk Keyboard Demos" showNavButton="true" navButtonPress=".onNavBack" class="sapUiContentPadding">
        <landmarkInfo>
            <PageAccessibleLandmarkInfo rootRole="None" headerRole="Banner" />
        </landmarkInfo>
        <content>
            <List
                headerText="Choose a Demo"
                items="{state>/kioskDemoEntries}"
                itemPress=".onListEntryNavigate">
                <StandardListItem
                    title="{state>title}"
                    description="{state>description}"
                    type="Navigation" />
            </List>
        </content>
    </Page>
</mvc:View>
