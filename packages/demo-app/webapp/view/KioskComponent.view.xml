<mvc:View
    controllerName="demo.hotkeys.controller.KioskComponent"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">
    <Page titleAlignment="Center" title="Component-Level Keyboard Demo" showNavButton="true" navButtonPress=".onNavBack" class="sapUiContentPadding demoDockedPage">
        <content>
            <Panel headerText="Persistent Keyboard" class="sapUiSmallMarginBottom">
                <VBox class="sapUiSmallMargin">
                    <Text text="This keyboard is created in the controller and placed in the sap-ui-static UIArea. It persists across navigation. Click 'Round-Trip Navigation' to navigate away and return after 2 seconds; the keyboard stays visible throughout." />
                    <Label text="Input Field" labelFor="compInput" class="sapUiSmallMarginTop" />
                    <Input id="compInput" placeholder="Type here..." width="100%" />
                    <HBox class="sapUiSmallMarginTop">
                        <Button text="Show Keyboard" press=".onShowKeyboard" type="Emphasized" class="sapUiSmallMarginEnd" />
                        <Button text="Close Keyboard" press=".onCloseKeyboard" class="sapUiSmallMarginEnd" />
                    </HBox>
                    <Button text="Round-Trip Navigation" press=".onNavigateAway" class="sapUiSmallMarginTop" />
                </VBox>
            </Panel>

            <Panel headerText="Status" class="sapUiSmallMarginBottom">
                <VBox class="sapUiSmallMargin">
                    <HBox>
                        <Label text="Keyboard isOpen():" class="sapUiSmallMarginEnd" />
                        <ObjectStatus text="{state>/kioskIsOpen}" state="Information" />
                    </HBox>
                    <HBox>
                        <Label text="Last Key:" class="sapUiSmallMarginEnd" />
                        <ObjectStatus text="{state>/kioskLastKey}" state="Information" />
                    </HBox>
                </VBox>
            </Panel>
        </content>
    </Page>
</mvc:View>
